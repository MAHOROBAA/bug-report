rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // reports 컬렉션: 누구나 읽기 + 쓰기 가능
    match /reports/{document=**} {
      allow read, write: if true;
    }

    // 다른 컬렉션 접근 금지
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
